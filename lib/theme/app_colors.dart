import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();
  static const primary = Color.fromRGBO(27, 110, 247, 1);
  static const primaryDeep = Color(0xFF007AFF);
  static const background = Color(0xFFFFFFFF);
  static const textDark = Color(0xFF1C1C1E);
  static const textLight = Color.fromRGBO(100, 116, 139, 1);
  static const success = Color(0xFF34C759);
  static const danger = Color(0xFFFF3B30);
  static const field = Color(0xFFF5F7FA);
}
